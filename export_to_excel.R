# export_to_excel.R
# Optional: export PAM or stage-specific matrices to Excel
# Only include if you actually ran this

# Example (using openxlsx):
# library(openxlsx)
# write.xlsx(pam_matrix_all, "PAM_matrix.xlsx", rowNames = TRUE)
# write.xlsx(mat_stage, "PAM_stage_matrix.xlsx", rowNames = TRUE)
